// Get todos

// Add stocks
const postStock = 'INSERT INTO stocks SET ?;';
const getStock = 'SELECT * FROM stocks;';

// Deleting todos

// Updating todos

module.exports = {
  postStock,
  getStock,
};
